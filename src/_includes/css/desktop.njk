<style>
    @media (min-width: 769px) {
        /* ========= 通用桌面端样式 ========= */
        /* 侧边栏 sticky（用于科目页面） */
        .sidebar {
            position: sticky;
            top: 2rem;
            align-self: start;
        }
        /* 隐藏移动端专用的悬浮按钮与折叠面板 */
        .mobile-menu-btn, .mobile-menu-panel {
            display: none;
        }
        /* 搜索框字体微调 */
        .search-box {
            font-size: 1rem;
        }
        /* 卡片过渡效果 */
        .subject-card {
            transition: transform 0.25s ease, box-shadow 0.25s ease;
        }

        /* ========= 学科页面文档卡片专用样式 ========= */
        /* 强制一行三列，限制最大宽度并居中，以留出两侧空白 */
        .doc-grid {
            grid-template-columns: repeat(3, 1fr); /* 强制三等分列 */
            max-width: 1000px; /* 设置最大宽度，防止卡片被拉得过宽 */
            margin: 0 auto;    /* 居中显示，留出两边空位 */
            gap: 2rem;         /* 保持卡片之间的舒适间距 */
        }

        /* ========= 主页左右分栏专用样式 (桌面端) ========= */
        .home-layout {
            flex-direction: row; /* 在电脑上变成左右横向排列 */
            align-items: center; /* 垂直居中，不再拉伸高度 */
            justify-content: center; /* 整体居中 */
            gap: 3rem; /* 左右两边留出足够的间距 */
        }

        .home-left {
            width: 35%; /* 调整左侧区域宽度 */
            display: flex;
            flex-direction: column; /* 改为纵向排列，让时间和寄语上下堆叠 */
            align-items: flex-end;  /* 让卡片整体靠右（靠近中线） */
            justify-content: center; /* 整体垂直居中，与右侧保持对称 */
            gap: 2.5rem; /* 设定时间与寄语的舒适间距，自然地把寄语往下推 */
        }

        .home-right {
            width: 55%; /* 右侧内容区宽度 */
        }

        /* 左侧时间与寄语卡片在大屏时统一尺寸 */
        .message-card,
        .time-card {
            width: 100%;
            max-width: 350px; /* 限制最大宽度，保持修长的形状 */
        }

        .message-card {
            padding: 2.5rem 2rem;
            /* 若想进一步调大寄语，可在这里覆盖 font-size */
        }

        /* 强制右下角的学科卡片网格为一行三列 */
        .home-right .subject-grid {
            grid-template-columns: repeat(3, 1fr);
            gap: 1.5rem;
            margin-top: 0;
        }

        /* 微调桌面端卡片的内部间距 */
        .home-right .subject-card {
            padding: 1.25rem;
        }
        .home-right .subject-icon {
            font-size: 2.2rem;
        }
        .home-right .subject-card h3 {
            font-size: 1.2rem;
        }
    }
</style>

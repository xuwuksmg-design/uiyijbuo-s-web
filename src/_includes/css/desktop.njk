<style>
    @media (min-width: 769px) {
        /* ========= 通用桌面端样式 ========= */
        /* 侧边栏 sticky（用于科目页面） */
        .sidebar {
            position: sticky;
            top: 2rem;
            align-self: start;
        }
        /* 隐藏移动端专用的悬浮按钮与折叠面板 */
        .mobile-menu-btn, .mobile-menu-panel {
            display: none;
        }
        /* 搜索框字体微调 */
        .search-box {
            font-size: 1rem;
        }
        /* 卡片过渡效果 */
        .subject-card {
            transition: transform 0.25s ease, box-shadow 0.25s ease;
        }

        /* ========= 学科页面文档卡片专用样式 ========= */
        /* 强制一行三列，限制最大宽度并居中，以留出两侧空白 */
        .doc-grid {
            grid-template-columns: repeat(3, 1fr); /* 强制三等分列 */
            max-width: 1000px; /* 设置最大宽度，防止卡片被拉得过宽 */
            margin: 0 auto;    /* 居中显示，留出两边空位 */
            gap: 2rem;         /* 保持卡片之间的舒适间距 */
        }

        /* ========= 主页专用样式 ========= */
        /* 主页卡片容器：整体靠右下 */
        .home-cards-wrapper {
            display: flex;
            justify-content: flex-end;  /* 整体向右对齐 */
            margin-top: 4rem;           /* 适当下移 */
            margin-bottom: 2rem;
            margin-right: 1.5rem;       /* 向右微调 */
        }

        /* 卡片网格强制三列两行 */
        .home-cards-wrapper .subject-grid {
            grid-template-columns: repeat(3, minmax(240px, 280px)); /* 固定宽度或自适应 */
            gap: 1rem;                  /* 缩小卡片上下左右缝隙 */
            max-width: 900px;           /* 限制最大宽度，避免太宽 */
            width: 100%;
        }

        /* 可选：减小卡片内边距，让卡片更紧凑 */
        .home-cards-wrapper .subject-card {
            padding: 1rem;              /* 原全局为1.5rem，这里缩小 */
        }
        .home-cards-wrapper .subject-icon {
            font-size: 2.2rem;          /* 原3rem，缩小 */
        }
        .home-cards-wrapper .subject-card h3 {
            font-size: 1.1rem;          /* 原1.4rem，缩小 */
        }
        .home-cards-wrapper .subject-card p {
            font-size: 0.8rem;          /* 原0.85rem，略小 */
        }
    }
</style>

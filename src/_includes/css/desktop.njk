<style>
    @media (min-width: 769px) {
        /* 侧边栏 sticky */
        .sidebar {
            position: sticky;
            top: 2rem;
            align-self: start;
        }
        /* 移动端底部导航隐藏 */
        .mobile-bottom-nav {
            display: none;
        }
        /* 桌面端可添加其他优化 */
        .search-box {
            font-size: 1rem;
        }
        .subject-card {
            transition: transform 0.25s ease, box-shadow 0.25s ease;
        }
    }
</style>
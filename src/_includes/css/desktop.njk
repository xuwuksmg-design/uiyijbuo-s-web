<style>
    @media (min-width: 769px) {
        /* ========= 通用桌面端样式 ========= */
        /* 侧边栏 sticky（用于科目页面） */
        .sidebar {
            position: sticky;
            top: 2rem;
            align-self: start;
        }
        /* 隐藏移动端专用的悬浮按钮与折叠面板 */
        .mobile-menu-btn, .mobile-menu-panel {
            display: none;
        }
        /* 搜索框字体微调 */
        .search-box {
            font-size: 1rem;
        }
        /* 卡片过渡效果 */
        .subject-card {
            transition: transform 0.25s ease, box-shadow 0.25s ease;
        }

        /* ========= 学科页面文档卡片专用样式 ========= */
        /* 强制一行三列，限制最大宽度并居中，以留出两侧空白 */
        .doc-grid {
            grid-template-columns: repeat(3, 1fr); /* 强制三等分列 */
            max-width: 1000px; /* 设置最大宽度，防止卡片被拉得过宽 */
            margin: 0 auto;    /* 居中显示，留出两边空位 */
            gap: 2rem;         /* 保持卡片之间的舒适间距 */
        }

                /* ========= 主页左右分栏专用样式 (桌面端) ========= */
        .home-layout {
            flex-direction: row; /* 在电脑上变成左右横向排列 */
            align-items: stretch; /* 让左边的小卡片自动拉伸对齐高度 */
        }

        .home-left {
            width: 30%; /* 左侧寄语区占 30% 宽度 */
            flex-shrink: 0;
        }

        .home-right {
            width: 70%; /* 右侧内容区占 70% 宽度 */
            flex-grow: 1;
        }

        /* 左侧寄语卡片在大屏时填满整个左侧高度 */
        .message-card {
            height: 100%;
            padding: 3rem;
            font-size: 1.4rem; /* 电脑上字稍微大一点更有感觉 */
        }

        /* 强制右下角的学科卡片网格为一行三列 */
        .home-right .subject-grid {
            grid-template-columns: repeat(3, 1fr);
            gap: 1.5rem;
            margin-top: 0; /* 配合 flex 的 gap */
        }

        /* 微调桌面端卡片的内部间距 */
        .home-right .subject-card {
            padding: 1.25rem;
        }
        .home-right .subject-icon {
            font-size: 2.2rem;
        }
        .home-right .subject-card h3 {
            font-size: 1.2rem;
        }
    }
</style>
